version: '3.8'

services:
  devtoolkit-backend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    environment:
      - SPRING_PROFILES_ACTIVE=docker
      - JWT_SECRET=your-256-bit-secret-key-here
      - SERVER_PORT=8080
    networks:
      - devtoolkit-backend-network
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/actuator/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

networks:
  devtoolkit-backend-network:
    driver: bridge 